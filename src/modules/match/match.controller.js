angular.module('mainapp.match')
    .controller('mainapp.match.MatchController', ['Api', 'AuthenticationService', '$location', '$rootScope', '$timeout',
        function(Api, AuthenticationService, $location, $rootScope, $timeout)
    {
        var self = this;

    }]);